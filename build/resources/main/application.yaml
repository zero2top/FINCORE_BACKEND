spring:
  application:
    name: fincore-backend
  datasource:
    url: jdbc:mysql://localhost:3306/banking_db?serverTimezone=Asia/Seoul
    username: root # 본인 MySQL 계정
    password: lsm110403! # 본인 MySQL 비번
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # Entity 만들면 자동으로 테이블 생성해줌 (매우 편함)
    show-sql: true
    properties:
      hibernate:
        format_sql: true